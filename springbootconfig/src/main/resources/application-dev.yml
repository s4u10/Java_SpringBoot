#DEV environment

server:
    port: 8085
app:
  message: This is the propert file to the ${spring.application.name} specific to DEV Environment

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jbbc:h2:mem:db;DE_CLOSE_DELAY=-1
    username: sa
    password: sa